/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
author: Elin (https://sketchfab.com/ElinHohler)
license: CC-BY-4.0 (http://creativecommons.org/licenses/by/4.0/)
source: https://sketchfab.com/3d-models/video-game-coin-062e04afa200434cb3ba96b112faf516
title: Video game Coin
*/

import React, { useEffect, useRef } from 'react'
import { useGLTF, useAnimations } from '@react-three/drei'
import gsap from 'gsap'
import { TweenMax } from 'gsap/gsap-core'
import { useFrame } from '@react-three/fiber'

export default function Model(props) {
  const group = useRef()
  const { nodes, materials, animations } = useGLTF('/coin.gltf')
  const { actions } = useAnimations(animations, group)
  const myMesh = React.useRef();

  useFrame(({ clock }) => {
    const a = clock.getElapsedTime();
    myMesh.current.rotation.z = a * 0.2;
  });

  return (
    <group ref={group} {...props} dispose={null}>
      <group name="Sketchfab_Scene">
        <group name="Sketchfab_model" rotation={[-Math.PI / 2, 0, 0]}>
          <group name="root">
            <group name="GLTF_SceneRootNode" rotation={[Math.PI / 2, 0, 0]}>
              <group name="Coin_0" position={[0, 0.51, 0]} rotation={[1.57, 0, 0]} scale={5}>
                <mesh ref={myMesh} name="Object_4" geometry={nodes.Object_4.geometry} material={materials.Coin} />
              </group>
            </group>
          </group>
        </group>
      </group>
    </group>
  )
}

useGLTF.preload('/coin.gltf')
